terraform {
  required_version = ">= 1.6.0"

  required_providers {
    # TODO: uncomment and pin the provider version
    # aws = {
    #   source  = "hashicorp/aws"
    #   version = "~> X.Y"
    # }
  }
}
